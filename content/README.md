# 网站文案管理

`website-content.json` 是官网文案的唯一内容源。

你可以直接编辑这个 JSON 文档，也可以运行 `npm run studio`，在本地内容后台中修改并发布。公开页面、产品迭代和 AI 沉思录都会从同一份内容读取，不需要再进入 React 组件寻找文案。AI 沉思录的 `body` 支持换行，可用来发布完整正文；留空时页面只显示摘要。

注意：本地后台的“发布”会写回这个文件并重新构建网站，但不会自动上传到互联网。公网发布需要后续再连接托管平台。
